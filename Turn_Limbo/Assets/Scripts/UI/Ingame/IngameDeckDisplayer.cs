using UnityEngine;
using UnityEngine.UI;

public class IngameDeckDisplayer : MonoBehaviour
{
    [SerializeField] private IngameDeckDisplayer[] displayers;

    private void Update()
    {
        for(int i = 0; i < displayers.Length; i++)
        {
            // if(controller.inputs)
        }
    }
}