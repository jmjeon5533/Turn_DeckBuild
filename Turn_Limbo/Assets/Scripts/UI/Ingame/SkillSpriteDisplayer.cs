using UnityEngine;
using UnityEngine.UI;

public class SkillSpriteDisplayer : MonoBehaviour
{
    [SerializeField] private Image[] firstSkillImages;
    [SerializeField] private Image[] secondSkillImages;

    private void Update()
    {
        for(int i = 0; i < 3; i++)
        {
            // if(controller.inputs)
        }
    }
}